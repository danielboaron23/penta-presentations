# Penta — Presentations

Two static presentation decks, viewable in any browser (no build needed).

**Open `index.html`** for the hub with a nav bar to switch between:

1. **Seller Profile Review** — `seller-profile-deck.html` — PM → Design Research → Senior Design, with a live preview of the seller profile (`profile.html` / `profile-mobile.html`).
2. **Skill Building** — `skill-building.html` — how the Penta PM & Design Research skills were built.

Deep links: `index.html#seller` and `index.html#skills`.

Built with the Penta design system (`design-system/`). Hosted on GitHub Pages.
